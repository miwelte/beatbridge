# beatbridge 
### RasPi 4 based "Audio BPM-Hold → MIDI-Controller"

<br>

## Python Application




### DevOps

`pip freeze > requirements.txt` \
<br>
`pip install -r requirements.txt` \
<br>
`pip freeze > requirements.txt` \
<br>
`pip freeze > requirements.txt` \
<br>
`pip freeze > requirements.txt`\
<br>